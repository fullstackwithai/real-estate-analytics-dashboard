const data = window.MARKET_DATA;
const citySelect=document.querySelector('#citySelect');const metricSelect=document.querySelector('#metricSelect');
function money(n){return new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n)}
function render(){const city=citySelect.value;const metric=metricSelect.value;const rows=data.filter(d=>d.city===city);const latest=rows.at(-1);const first=rows[0];const change=((latest[metric]-first[metric])/first[metric]*100).toFixed(1);document.querySelector('#kpis').innerHTML=`<div class="kpi"><span class="muted">Median price</span><strong>${money(latest.medianPrice)}</strong></div><div class="kpi"><span class="muted">Active listings</span><strong>${latest.listings}</strong></div><div class="kpi"><span class="muted">Avg. days on market</span><strong>${latest.avgDays}</strong></div><div class="kpi"><span class="muted">Metric change</span><strong>${change}%</strong></div>`;drawChart(rows,metric);drawTable(rows)}
function drawChart(rows,metric){const svg=document.querySelector('#chart');const w=760,h=310,p=42;const vals=rows.map(r=>r[metric]);const min=Math.min(...vals)*.96,max=Math.max(...vals)*1.04;const pts=rows.map((r,i)=>{const x=p+i*(w-p*2)/(rows.length-1);const y=h-p-((r[metric]-min)/(max-min))*(h-p*2);return [x,y,r]});svg.setAttribute('viewBox',`0 0 ${w} ${h}`);const line=pts.map((p,i)=>(i?'L':'M')+p[0]+','+p[1]).join(' ');svg.innerHTML=`<defs><linearGradient id="fill" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#38bdf8" stop-opacity=".35"/><stop offset="1" stop-color="#38bdf8" stop-opacity="0"/></linearGradient></defs><path d="${line} L ${pts.at(-1)[0]},${h-p} L ${pts[0][0]},${h-p} Z" fill="url(#fill)"/><path d="${line}" fill="none" stroke="#38bdf8" stroke-width="5" stroke-linecap="round"/>${pts.map(pt=>`<circle cx="${pt[0]}" cy="${pt[1]}" r="7" fill="#0b1220" stroke="#38bdf8" stroke-width="4"/><text x="${pt[0]}" y="${h-12}" fill="#a7b4c8" text-anchor="middle" font-size="14">${pt[2].month}</text>`).join('')}`;}
function drawTable(rows){document.querySelector('#tbody').innerHTML=rows.map(r=>`<tr><td>${r.month}</td><td>${money(r.medianPrice)}</td><td>${r.listings}</td><td>${r.avgDays}</td><td>${money(r.rent)}</td></tr>`).join('')}
[citySelect,metricSelect].forEach(el=>el.addEventListener('change',render));render();
